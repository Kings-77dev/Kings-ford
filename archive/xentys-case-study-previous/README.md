# Previous Xentys case study

This directory preserves the Xentys case-study implementation that existed at
the current repository `HEAD` before the approved replacement was connected to
`/work/xentys`.

Archived source:

- `components/work/XentysCaseStudy.tsx`: previous Xentys case-study body.
- `app/work/[slug]/page.tsx`: previous shared project-page wrapper and Xentys
  hero treatment.

These files are reference copies only. Nothing in the active Next.js application
imports from `archive/`, so this version cannot affect the live site or build.

The active replacement remains in:

- `/components/work/XentysCaseStudy.tsx`
- `/app/work/[slug]/page.tsx`

